╔══════════════════════════════════════════════════════════════════════╗
║              EC2 MIGRATION FIX - QUICK REFERENCE CARD                    ║
╚══════════════════════════════════════════════════════════════════════════╝

🔴 ERROR YOU'RE SEEING:
   "column malware_scan_status of relation claims already exists"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ QUICK FIX (Just 4 commands on EC2):

   ssh -i your-key.pem ubuntu@your-ec2-ip
   cd ~/fyp_system
   git pull origin main
   ./ec2-quick-migration-fix.sh

   ⚡ That's it! The script does everything automatically.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📦 WHAT THE SCRIPT DOES AUTOMATICALLY:

   ✓ Verifies the migration fix is present
   ✓ Checks if malware_scan_status column exists
   ✓ Marks old migration as complete (if needed)
   ✓ Installs npm dependencies
   ✓ Rebuilds TypeScript
   ✓ Runs new HR module migrations
   ✓ Creates employees & employee_documents tables
   ✓ Restarts backend with PM2
   ✓ Displays verification steps

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 VERIFY IT WORKED:

   1. Check migrations:       cd backend && npm run typeorm migration:show
   2. Check HR tables exist:  npm run typeorm query "\\dt"
   3. Check backend running:  pm2 logs backend
   4. Test health endpoint:   curl http://localhost:3000/health

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📚 MORE DETAILS (Optional Reading):

   - MIGRATION_ERROR_RESOLUTION.md    → Why this happened & full explanation
   - EC2_MIGRATION_FINAL_FIX.md       → Detailed step-by-step instructions
   - ec2-quick-migration-fix.sh       → View the automated script source
   - HR_MODULE_DEPLOYMENT_GUIDE.md    → Complete HR module setup

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 AFTER THE FIX:

   1. Run HR tests:            ./test-hr-module.sh
   2. Create HR test user:     (see HR_MODULE_DEPLOYMENT_GUIDE.md)
   3. Test file uploads:       (with ClamAV scanning)
   4. Deploy frontend:         cd frontend && npm run build && pm2 restart frontend

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

❓ WHY DID THIS HAPPEN?

   The migration created the column previously, but wasn't recorded in the
   migrations table. The fix adds existence checks so it's safe to re-run.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⏱️  TIME TO FIX: ~3-5 minutes

✨ ALL CHANGES ARE ALREADY PUSHED TO GITHUB - just pull and run!

╚══════════════════════════════════════════════════════════════════════════╝
