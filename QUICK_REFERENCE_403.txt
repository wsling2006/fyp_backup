╔═══════════════════════════════════════════════════════════════════════════╗
║                   🚨 403 ERROR - QUICK REFERENCE CARD 🚨                  ║
╚═══════════════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────────────┐
│ INSTANT FIX (Try this first - Fixes 95% of issues)                     │
└─────────────────────────────────────────────────────────────────────────┘

1. Logout from app
2. Press Ctrl+Shift+Delete → Clear cache
3. Close browser completely
4. Reopen browser
5. Login again
6. Navigate to /purchase-requests

┌─────────────────────────────────────────────────────────────────────────┐
│ VERIFY ISSUE (30 seconds) - Browser Console (F12)                      │
└─────────────────────────────────────────────────────────────────────────┘

Paste this in browser console:
────────────────────────────────────────────────────────────────────────────
const t = localStorage.getItem('token');
if (t) {
  const p = JSON.parse(atob(t.split('.')[1]));
  console.log('Role:', p.role);
  console.log('Expires:', new Date(p.exp * 1000));
  console.log('Valid:', p.role === 'sales_department' && new Date() < new Date(p.exp * 1000));
}
────────────────────────────────────────────────────────────────────────────

✅ Expected: Valid: true, Role: sales_department
❌ If false: Clear cache & re-login

┌─────────────────────────────────────────────────────────────────────────┐
│ CHECK DATABASE ROLE (On EC2)                                           │
└─────────────────────────────────────────────────────────────────────────┘

sudo -u postgres psql -d fyp_db -c \
  "SELECT email, role FROM users WHERE email='YOUR_EMAIL';"

✅ Expected: role = 'sales_department' (exact match)
❌ If wrong: Run this to fix:

sudo -u postgres psql -d fyp_db -c \
  "UPDATE users SET role='sales_department' WHERE email='YOUR_EMAIL';"

Then user MUST logout and re-login!

┌─────────────────────────────────────────────────────────────────────────┐
│ VERIFY AUTHORIZATION HEADER                                             │
└─────────────────────────────────────────────────────────────────────────┘

1. Open DevTools (F12) → Network tab
2. Navigate to /purchase-requests
3. Find API request → Click it → Headers tab
4. Look for: Authorization: Bearer <long-token>

✅ Header present → Token is being sent
❌ Header missing → Clear cache & re-login

┌─────────────────────────────────────────────────────────────────────────┐
│ CHECK BACKEND LOGS (On EC2)                                             │
└─────────────────────────────────────────────────────────────────────────┘

pm2 logs backend --lines 50

Look for:
- JWT validation errors
- Role mismatch errors
- "Insufficient permissions"

┌─────────────────────────────────────────────────────────────────────────┐
│ RESTART SERVICES (After code changes)                                  │
└─────────────────────────────────────────────────────────────────────────┘

cd /home/ubuntu/fyp_system
cd backend && npm run build
cd ../frontend && npm run build
pm2 restart ecosystem.config.js

┌─────────────────────────────────────────────────────────────────────────┐
│ ROLE-BASED ACCESS MATRIX                                               │
└─────────────────────────────────────────────────────────────────────────┘

Role              │ View Requests │ Create │ Review │ Upload Receipt
──────────────────┼───────────────┼────────┼────────┼───────────────
sales_department  │ ✅ Own only    │ ✅ Yes  │ ❌ No   │ ✅ Own only
marketing         │ ✅ Own only    │ ✅ Yes  │ ❌ No   │ ✅ Own only
accountant        │ ✅ All         │ ❌ No   │ ✅ Yes  │ ❌ No
super_admin       │ ✅ All         │ ✅ Yes  │ ✅ Yes  │ ✅ All

┌─────────────────────────────────────────────────────────────────────────┐
│ VALID ROLE VALUES (Must match exactly)                                 │
└─────────────────────────────────────────────────────────────────────────┘

✅ sales_department   ← Correct
✅ marketing          ← Correct
✅ accountant         ← Correct
✅ human_resources    ← Correct
✅ super_admin        ← Correct

❌ sales             ← Wrong (missing _department)
❌ SALES_DEPARTMENT  ← Wrong (must be lowercase)
❌ sale_department   ← Wrong (typo)

┌─────────────────────────────────────────────────────────────────────────┐
│ ERROR MESSAGES & FIXES                                                  │
└─────────────────────────────────────────────────────────────────────────┘

"403 Forbidden"
→ Role check failed
→ FIX: Check JWT role, clear cache, re-login

"401 Unauthorized"
→ Token invalid/expired
→ FIX: Re-login

"User not authenticated"
→ No token sent
→ FIX: Check localStorage, re-login

"Insufficient permissions"
→ Wrong role in JWT
→ FIX: Check database role, re-login

┌─────────────────────────────────────────────────────────────────────────┐
│ VERIFICATION SCRIPTS                                                    │
└─────────────────────────────────────────────────────────────────────────┘

Frontend (Browser):
  cat verify-frontend-jwt.sh

Backend (EC2):
  ./verify-backend-jwt.sh

┌─────────────────────────────────────────────────────────────────────────┐
│ DOCUMENTATION FILES                                                     │
└─────────────────────────────────────────────────────────────────────────┘

QUICK_FIX_403.md           → 5-min troubleshooting
FINAL_403_DEBUG_GUIDE.md   → Complete technical guide
FINAL_SUMMARY_403.md       → Executive summary
AUTH_FLOW_VISUAL.md        → Visual flowcharts

┌─────────────────────────────────────────────────────────────────────────┐
│ CHECKLIST                                                               │
└─────────────────────────────────────────────────────────────────────────┘

□ Logged out completely
□ Cleared browser cache
□ Closed and reopened browser
□ Re-logged in
□ Verified JWT token in localStorage
□ Checked JWT role is 'sales_department'
□ Verified Authorization header in Network tab
□ Checked database user role
□ Tested purchase requests page
□ No errors in backend logs

┌─────────────────────────────────────────────────────────────────────────┐
│ STILL STUCK? Provide This Info:                                        │
└─────────────────────────────────────────────────────────────────────────┘

1. JWT payload (from browser console)
2. Screenshot of Network tab (failed request)
3. Backend logs: pm2 logs backend --lines 50
4. Database user: SELECT * FROM users WHERE email='YOUR_EMAIL';

╔═══════════════════════════════════════════════════════════════════════════╗
║ ✅ Code is 100% correct - This is a token/cache issue                     ║
║ 🎯 Fix: Logout + Clear cache + Re-login                                   ║
╚═══════════════════════════════════════════════════════════════════════════╝
