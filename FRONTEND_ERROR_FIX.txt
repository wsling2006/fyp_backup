╔═══════════════════════════════════════════════════════════════════════════╗
║                   🚨 FRONTEND ERRORED - QUICK FIX 🚨                      ║
╚═══════════════════════════════════════════════════════════════════════════╝

PROBLEM DETECTED:
  PM2 Status shows:
  - Frontend: status = errored (shown as ...90m)
  - Memory: 0b (indicates crash)
  - Restarts: 13 (keeps crashing)

ROOT CAUSE:
  Frontend build failed or process crashed, PM2 keeps trying to restart it

═══════════════════════════════════════════════════════════════════════════

🎯 IMMEDIATE FIX (2 commands)

On EC2:
  1. cd /home/ubuntu/fyp_system
  2. ./fix-frontend-error.sh

This will:
  ✅ Stop all PM2 processes
  ✅ Pull latest code
  ✅ Clear build cache
  ✅ Reinstall dependencies
  ✅ Rebuild frontend from scratch
  ✅ Restart all services
  ✅ Save PM2 config

═══════════════════════════════════════════════════════════════════════════

📋 STEP-BY-STEP MANUAL FIX

If script fails, run these commands manually:

  # Stop and delete all processes
  pm2 stop all
  pm2 delete all

  # Pull latest code
  cd /home/ubuntu/fyp_system
  git pull origin main

  # Clean and rebuild frontend
  cd frontend
  rm -rf .next node_modules/.cache
  npm install
  npm run build

  # Start services
  cd ..
  pm2 start ecosystem.config.js
  pm2 save

  # Check status
  pm2 status
  pm2 logs

═══════════════════════════════════════════════════════════════════════════

🔍 DIAGNOSTIC (If you want to see the error first)

  ./check-frontend-error.sh

This shows:
  - PM2 status
  - Frontend error logs
  - Build folder status
  - Ecosystem config

═══════════════════════════════════════════════════════════════════════════

✅ SUCCESS INDICATORS

After running fix, you should see:

  pm2 status:
  ┌────┬──────────┬──────┬──────┬────────┬─────────┬──────────┐
  │ id │ name     │ mode │ ↺    │ status │ cpu     │ memory   │
  ├────┼──────────┼──────┼──────┼────────┼─────────┼──────────┤
  │ 0  │ backend  │ fork │ 0    │ online │ 0%      │ 85.0mb   │
  │ 1  │ frontend │ fork │ 0    │ online │ 0%      │ 75.0mb   │  ← Should show "online" with real memory
  └────┴──────────┴──────┴──────┴────────┴─────────┴──────────┘

  Key indicators:
  ✅ status: online (not errored)
  ✅ memory: >50mb (not 0b)
  ✅ restarts (↺): 0 (not continuously restarting)

═══════════════════════════════════════════════════════════════════════════

🚨 COMMON ERRORS & FIXES

ERROR: "Module not found: Can't resolve '@/context/AuthContext'"
FIX: Frontend build incomplete or tsconfig wrong
→ Run: ./fix-frontend-error.sh

ERROR: "EADDRINUSE: address already in use"
FIX: Port 3001 already taken
→ Run: pm2 delete all && ./fix-frontend-error.sh

ERROR: "npm install failed"
FIX: Corrupted package-lock.json
→ Run: rm frontend/package-lock.json && npm install

ERROR: Frontend keeps restarting
FIX: Build failed or syntax error in code
→ Check logs: pm2 logs frontend --lines 100

═══════════════════════════════════════════════════════════════════════════

📞 VERIFICATION COMMANDS

  # Check status
  pm2 status

  # Monitor logs in real-time
  pm2 logs

  # Check specific service
  pm2 logs frontend --lines 50

  # Test locally
  curl http://localhost:3001

  # Check processes
  pm2 list

═══════════════════════════════════════════════════════════════════════════

⚡ QUICK COMMANDS REFERENCE

  # Fix everything (RECOMMENDED)
  ./fix-frontend-error.sh

  # Just check the error
  ./check-frontend-error.sh

  # Restart services
  pm2 restart all

  # View logs
  pm2 logs

  # Check which port is in use
  netstat -tulpn | grep 3001

  # Kill process on port (if needed)
  sudo fuser -k 3001/tcp

═══════════════════════════════════════════════════════════════════════════

🎯 YOUR NEXT STEP

  SSH into EC2 and run:

    cd /home/ubuntu/fyp_system
    git pull origin main
    chmod +x *.sh
    ./fix-frontend-error.sh

  Expected time: 5-10 minutes

═══════════════════════════════════════════════════════════════════════════

✅ After successful fix:
  1. Open browser: http://13.212.147.123:3001
  2. Clear cache (Ctrl+Shift+Delete)
  3. Login and test all features
  4. Verify no errors in console

═══════════════════════════════════════════════════════════════════════════
